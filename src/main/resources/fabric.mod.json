{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": ["${mod_author}"],
  "contact": {
    "homepage": "${mod_url}",
    "sources": "${mod_source}"
  },
  "license": "${mod_license}",
  "icon": "assets/${mod_id}/${mod_icon}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "top.theillusivec4.cakechomps.CakeChomps"
    ]
  },
  "depends": {
    "fabricloader": ">=0.4.0",
    "fabric": "*"
  }
}